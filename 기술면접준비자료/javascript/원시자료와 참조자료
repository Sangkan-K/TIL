    원시 자료형은 자바스크립트에서 number,string,boolean과 같은 고정된 저장 공간을 차지하는
    데이터를 말합니다. 원시 자료형은 객체가 아니면서 메소드를 가지지 않는 6가지 타입입니다.
    string, number, boolean, undefined, symbol, null이 이에 해당합니다.

    원시 자료형이 아닌 모든 데이터는 참조 자료형입니다.
    참조 자료형의 대표로는 배열, 객체, 함수를 말할 수 있습니다.

    원시 자료형이 할당 될 때에는 변수에 값 자체가 담기고, 참조 자료형이 할당될 때는 보관함의 주소가 담깁니다.
    그래서 각 변수 간에 데이터를 복사할 경우, 원시 자료는 데이터 값을 복사하기 때문에 기존의 데이터에 영향이 가지 않지만
    참조 자료는 데이터의 주소를 복사하기 때문에 복사한 데이터의 원소를 변경하면, 기존의 데이터에도 영향을 미치게 됩니다.